# 区块链证书存证系统 - 项目总结

## 📋 项目概述

这是一个基于区块链技术的证书存证系统，使用 FISCO BCOS 区块链和 IPFS 分布式存储，实现证书的防篡改存储和验证。

### 技术栈

**后端**:
- Spring Boot 2.7.18
- Java 17
- MyBatis Plus 3.5.3
- MySQL 8.0
- Redis
- JWT 认证
- FISCO BCOS SDK 2.9.2
- IPFS HTTP API

**前端**:
- Vue 3
- TypeScript
- Element Plus
- Vite

**区块链**:
- FISCO BCOS 2.9.1
- Solidity 0.4.25

**存储**:
- IPFS (Kubo v0.24.0)
- MySQL

## 🎯 核心功能

### 1. 用户管理
- ✅ 用户注册/登录（JWT）
- ✅ 角色权限管理（学生/教师/管理员）
- ✅ 个人信息管理
- ✅ 密码修改

### 2. 证书申请流程
- ✅ 学生提交证书申请
- ✅ 上传证明材料（存储到 IPFS）
- ✅ 多级审批流程
- ✅ 申请状态跟踪

### 3. 证书管理
- ✅ 证书生成（PDF）
- ✅ 证书上链存证（FISCO BCOS）
- ✅ 证书文件存储（IPFS）
- ✅ 证书查询和下载
- ✅ 证书撤销

### 4. 证书验证
- ✅ 通过证书编号验证
- ✅ 通过文件哈希验证
- ✅ 区块链数据验证
- ✅ 防篡改检测

### 5. 系统管理
- ✅ 学院/专业管理
- ✅ 证书模板管理
- ✅ 系统日志
- ✅ 统计分析

## 🏗️ 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                        前端 (Vue 3)                          │
│                    Element Plus UI                           │
└────────────────────┬────────────────────────────────────────┘
                     │ HTTP/REST API
┌────────────────────▼────────────────────────────────────────┐
│                   后端 (Spring Boot)                         │
│  ┌──────────────┬──────────────┬──────────────────────────┐ │
│  │ Controller   │  Service     │  Repository              │ │
│  └──────┬───────┴──────┬───────┴──────┬───────────────────┘ │
│         │              │              │                      │
│  ┌──────▼──────┐ ┌────▼─────┐  ┌────▼──────┐              │
│  │ Security    │ │Blockchain│  │   IPFS    │              │
│  │  (JWT)      │ │ Service  │  │  Client   │              │
│  └─────────────┘ └────┬─────┘  └────┬──────┘              │
└───────────────────────┼─────────────┼───────────────────────┘
                        │             │
        ┌───────────────▼─────┐  ┌───▼──────────────┐
        │  FISCO BCOS 区块链  │  │  IPFS 分布式存储  │
        │  - 证书存证         │  │  - 证书文件       │
        │  - 智能合约         │  │  - 证明材料       │
        └─────────────────────┘  └──────────────────┘
                        │
        ┌───────────────▼─────────────────┐
        │         MySQL 数据库             │
        │  - 用户数据                      │
        │  - 申请记录                      │
        │  - 审批流程                      │
        └──────────────────────────────────┘
```

## 🔐 区块链功能详解

### 智能合约 (CertificateRegistry.sol)

**核心功能**:
1. **存储证书** (`storeCertificate`)
   - 证书编号
   - 文件哈希（SHA-256）
   - 颁发者地址
   - 存证时间戳

2. **验证证书** (`verifyCertificate`)
   - 检查证书是否存在
   - 验证文件哈希是否匹配
   - 检查是否已撤销
   - 返回存证时间

3. **撤销证书** (`revokeCertificate`)
   - 只有颁发者或管理员可撤销
   - 记录撤销时间
   - 触发撤销事件

4. **权限管理**
   - 合约所有者
   - 授权颁发者列表
   - 添加/移除授权

### 区块链集成流程

```
证书申请 → 审批通过 → 生成证书 → 上传IPFS → 计算哈希 → 上链存证
                                                    ↓
                                            智能合约记录
                                            - 证书编号
                                            - 文件哈希
                                            - 时间戳
                                                    ↓
                                            返回交易哈希
                                                    ↓
                                            保存到数据库
```

### 验证流程

```
用户上传证书 → 计算文件哈希 → 查询区块链 → 对比哈希值
                                    ↓
                            验证结果：
                            - 有效：哈希匹配且未撤销
                            - 无效：哈希不匹配或已撤销
                            - 不存在：链上无记录
```

## 📦 本地开发环境

### 已完成的环境搭建

#### 1. IPFS 环境 ✅
- **Docker 容器**: `ipfs-node`
- **API 端口**: 5001
- **Gateway 端口**: 8081
- **状态**: 运行正常
- **测试**: 文件上传下载功能正常

#### 2. FISCO BCOS 环境 ✅
- **Docker 容器**: `fisco-node`
- **Channel 端口**: 20200 (SDK 连接)
- **JSON-RPC 端口**: 8545
- **P2P 端口**: 30300
- **状态**: 运行正常
- **测试**: JSON-RPC 接口可访问

#### 3. 后端配置 ✅
- **区块链功能**: 已启用 (`blockchain.enabled: true`)
- **IPFS 功能**: 已启用 (`ipfs.enabled: true`)
- **SDK 证书**: 已配置到 `backend/src/main/resources/fisco/`
- **编译状态**: 成功

### 待完成任务

#### 1. 部署智能合约 🔄

**方法 1: 使用 Java 部署工具**
```bash
cd backend
mvn spring-boot:run -Dspring-boot.run.profiles=deploy
```

**方法 2: 使用 FISCO BCOS 控制台**
```bash
# 下载控制台
cd docker/fisco
curl -#LO https://github.com/FISCO-BCOS/console/releases/download/v2.9.2/download_console.sh
bash download_console.sh

# 配置控制台
cd console
# 复制 SDK 证书
cp ../nodes/127.0.0.1/sdk/* conf/

# 启动控制台
bash start.sh

# 部署合约
[group:1]> deploy CertificateRegistry
```

部署成功后，将合约地址配置到 `application.yml`:
```yaml
fisco:
  contract:
    address: 0x... # 部署后的合约地址
```

#### 2. 测试区块链功能 📝

启动后端服务：
```bash
cd backend
mvn spring-boot:run
```

测试接口：
- 创建证书申请
- 审批通过
- 生成证书（自动上链）
- 验证证书

#### 3. 环境切换配置 🔧

**本地环境** (application.yml):
```yaml
blockchain:
  enabled: true
ipfs:
  enabled: true
  host: localhost
  port: 5001
fisco:
  nodes:
    - 127.0.0.1:20200
```

**远程环境** (application-prod.yml):
```yaml
blockchain:
  enabled: true
ipfs:
  enabled: true
  host: 139.196.72.119
  port: 5001
fisco:
  nodes:
    - 139.196.72.119:20200
```

切换方式：
```bash
# 使用本地环境
mvn spring-boot:run

# 使用远程环境
mvn spring-boot:run -Dspring-boot.run.profiles=prod
```

## 📚 文档说明

### 已创建的文档

1. **docker/README.md** - 本地开发环境部署指南
   - IPFS 部署和配置
   - FISCO BCOS 部署和配置
   - 环境切换说明
   - 故障排查

2. **backend/deploy-contract.sh** - 合约部署脚本（Linux/Mac）

3. **backend/src/main/java/.../DeployContractApplication.java** - Java 部署工具

4. **backend/src/main/resources/application-deploy.yml** - 部署专用配置

## 🔍 关键代码位置

### 区块链相关
- **智能合约**: `backend/src/main/resources/contracts/CertificateRegistry.sol`
- **合约部署**: `backend/src/main/java/.../blockchain/ContractDeployer.java`
- **合约包装**: `backend/src/main/java/.../blockchain/CertificateContract.java`
- **区块链服务**: `backend/src/main/java/.../service/BlockchainService.java`
- **区块链配置**: `backend/src/main/java/.../config/BlockchainConfig.java`

### IPFS 相关
- **IPFS 接口**: `backend/src/main/java/.../ipfs/IpfsClient.java`
- **真实实现**: `backend/src/main/java/.../ipfs/RealIpfsClient.java`
- **Mock 实现**: `backend/src/main/java/.../ipfs/MockIpfsClient.java`
- **IPFS 服务**: `backend/src/main/java/.../service/IpfsService.java`
- **IPFS 配置**: `backend/src/main/java/.../config/IpfsConfig.java`

### 证书相关
- **证书服务**: `backend/src/main/java/.../service/CertificateService.java`
- **验证服务**: `backend/src/main/java/.../service/VerificationService.java`
- **PDF 生成**: `backend/src/main/java/.../util/PdfGenerator.java`
- **证书编号**: `backend/src/main/java/.../util/CertificateNumberGenerator.java`

## 🚀 快速开始

### 1. 启动基础服务

```bash
# 启动 MySQL 和 Redis（如果使用 Docker）
docker-compose up -d mysql redis

# 启动 IPFS
cd docker/ipfs
docker-compose up -d

# 启动 FISCO BCOS
cd docker/fisco
docker-compose up -d
```

### 2. 初始化数据库

```bash
# 执行初始化脚本
mysql -u root -p < 初始化数据库.sql
```

### 3. 部署智能合约

```bash
cd backend
mvn spring-boot:run -Dspring-boot.run.profiles=deploy
```

记录合约地址并配置到 `application.yml`。

### 4. 启动后端

```bash
cd backend
mvn spring-boot:run
```

### 5. 启动前端

```bash
cd frontend
npm install
npm run dev
```

访问: http://localhost:5173

## 📊 系统状态

### 已完成 ✅
- [x] 后端框架搭建
- [x] 前端框架搭建
- [x] 用户认证系统
- [x] 证书申请流程
- [x] 审批流程
- [x] 证书生成（PDF）
- [x] IPFS 本地环境
- [x] FISCO BCOS 本地环境
- [x] 真实 IPFS 客户端实现
- [x] 区块链配置和集成
- [x] 智能合约编写
- [x] 合约 Java 包装类
- [x] 环境配置文档

### 待完成 🔄
- [ ] 智能合约部署
- [ ] 区块链功能测试
- [ ] 完整流程测试
- [ ] 性能优化
- [ ] 前端完善

### 可选功能 💡
- [ ] WeBase 管理平台集成
- [ ] 证书批量导入
- [ ] 数据统计报表
- [ ] 邮件通知
- [ ] 移动端适配

## 🔧 配置说明

### 环境变量

可以通过环境变量覆盖配置：

```bash
# IPFS 配置
export IPFS_HOST=localhost
export IPFS_PORT=5001

# FISCO BCOS 配置
export FISCO_NODES=127.0.0.1:20200
export CONTRACT_ADDRESS=0x...

# 数据库配置
export DB_HOST=localhost
export DB_PORT=3306
export DB_NAME=certificate_system
export DB_USER=root
export DB_PASSWORD=root123456
```

### 配置文件优先级

1. 环境变量
2. application-{profile}.yml
3. application.yml

## 📞 技术支持

### 常见问题

**Q: IPFS 连接失败？**
A: 检查 Docker 容器是否运行，端口是否正确。

**Q: FISCO BCOS 连接失败？**
A: 检查证书文件是否正确，节点是否运行。

**Q: 合约部署失败？**
A: 检查节点连接，查看日志文件。

### 日志位置

- 后端日志: `backend/logs/certificate-system.log`
- IPFS 日志: `docker logs ipfs-node`
- FISCO 日志: `docker logs fisco-node`

## 📝 下一步计划

1. **立即执行**:
   - 部署智能合约
   - 测试区块链功能
   - 验证完整流程

2. **短期优化**:
   - 性能测试和优化
   - 错误处理完善
   - 日志系统优化

3. **长期规划**:
   - 多链支持
   - 跨链互操作
   - 隐私保护增强

## 🎓 学习资源

- [FISCO BCOS 官方文档](https://fisco-bcos-documentation.readthedocs.io/)
- [IPFS 官方文档](https://docs.ipfs.tech/)
- [Solidity 文档](https://docs.soliditylang.org/)
- [Spring Boot 文档](https://spring.io/projects/spring-boot)

---

**项目状态**: 开发中 🚧  
**最后更新**: 2025-12-17  
**版本**: 1.0.0-SNAPSHOT